# Configuration for the core threaded regression smoke target.
#
# Keep this file narrowly focused on making pg_regress temp clusters exercise
# thread-per-session backend startup and teardown. The test schedule decides
# which SQL surfaces are currently admitted to this smoke.
multithreaded = on
io_method = sync
summarize_wal = off
